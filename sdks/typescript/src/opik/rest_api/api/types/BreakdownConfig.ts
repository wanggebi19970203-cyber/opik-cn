// 此文件由 Fern 根据我们的 API 定义自动生成。

import type * as OpikApi from "../index.js";

export interface BreakdownConfig {
    field?: OpikApi.BreakdownConfigField;
    metadataKey?: string;
    subMetric?: string;
}
