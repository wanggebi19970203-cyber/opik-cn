---
description: 从这里开始将 Opik 集成到基于 Smolagents 的生成式 AI 应用中，实现端到端的 LLM 可观测性、单元测试和优化。
headline: Smolagents | Opik 文档
og:description: 使用 Smolagents 与 Opik 创建强大的 AI 代理。轻松构建、配置和使用多种工具及 LLM 提供商。
og:site_name: Opik 文档
og:title: 使用 Smolagents 构建 AI 代理 - Opik
title: Smolagents 的可观测性 - Opik
---

[Smolagents](https://huggingface.co/docs/smolagents/en/index) 是 HuggingFace 推出的框架，允许您创建具有各种能力的 AI 代理。

该框架提供了一种简单的方式来构建代理，可以执行编程、网页搜索等任务，并内置支持多种工具和 LLM 提供商。

## 账户设置

[Comet](https://www.comet.com/site?from=llm&utm_source=opik&utm_medium=colab&utm_content=smolagents&utm_campaign=opik) 提供 Opik 平台的托管版本，[只需创建账户](https://www.comet.com/signup?from=llm&utm_source=opik&utm_medium=colab&utm_content=smolagents&utm_campaign=opik) 并获取您的 API 密钥。

> 您也可以在本地运行 Opik 平台，更多信息请参阅[安装指南](https://www.comet.com/docs/opik/self-host/overview/?from=llm&utm_source=opik&utm_medium=colab&utm_content=smolagents&utm_campaign=opik)。

## 快速开始

### 安装

要将 Smolagents 集成与 Opik 配合使用，您需要安装 Smolagents 及所需的 OpenTelemetry 包：

```bash
pip install --upgrade opik 'smolagents[telemetry,toolkit]' opentelemetry-sdk opentelemetry-exporter-otlp
```

### 配置 Smolagents

要使用 Smolagents，您需要配置 LLM 提供商的 API 密钥。本示例使用 OpenAI。您可以在[这些页面中查找或创建 API 密钥](https://platform.openai.com/settings/organization/api-keys)：

可以通过环境变量设置：

```bash
export OPENAI_API_KEY="YOUR_API_KEY"
```

或者通过代码设置：

```python
import os
import getpass

if "OPENAI_API_KEY" not in os.environ:
    os.environ["OPENAI_API_KEY"] = getpass.getpass("Enter your OpenAI API key: ")
```

### 配置 OpenTelemetry

您需要设置以下环境变量来配置 OpenTelemetry，以便将数据发送到 Opik：

<Tabs>
    <Tab value="Opik Cloud" title="Opik Cloud">
        如果您使用 Opik Cloud，需要设置以下环境变量：
            
        ```bash wordWrap
        export OTEL_EXPORTER_OTLP_ENDPOINT=https://www.comet.com/opik/api/v1/private/otel
        export OTEL_EXPORTER_OTLP_HEADERS='Authorization=<your-api-key>,Comet-Workspace=default'
        ```

        <Tip>
            要将跟踪数据记录到特定项目，可以在 `OTEL_EXPORTER_OTLP_HEADERS`
            环境变量中添加 `projectName` 参数：

            ```bash wordWrap
            export OTEL_EXPORTER_OTLP_HEADERS='Authorization=<your-api-key>,Comet-Workspace=default,projectName=<your-project-name>'
            ```

            您也可以将 `Comet-Workspace` 参数更新为其他值，
            以便将数据记录到不同的工作空间。
        </Tip>
    </Tab>
    <Tab value="Enterprise deployment" title="企业部署">
        如果您使用 Opik 企业部署版，需要设置以下环境变量：

        ```bash wordWrap
        export OTEL_EXPORTER_OTLP_ENDPOINT=https://<comet-deployment-url>/opik/api/v1/private/otel
        export OTEL_EXPORTER_OTLP_HEADERS='Authorization=<your-api-key>,Comet-Workspace=default'
        ```

        <Tip>
            要将跟踪数据记录到特定项目，可以在 `OTEL_EXPORTER_OTLP_HEADERS`
            环境变量中添加 `projectName` 参数：

            ```bash wordWrap
            export OTEL_EXPORTER_OTLP_HEADERS='Authorization=<your-api-key>,Comet-Workspace=default,projectName=<your-project-name>'
            ```

            您也可以将 `Comet-Workspace` 参数更新为其他值，
            以便将数据记录到不同的工作空间。
        </Tip>
    </Tab>
    <Tab value="Self-hosted instance" title="自托管实例">

    如果您自托管 Opik，需要设置以下环境变量：

    ```bash
    export OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:5173/api/v1/private/otel
    ```

    <Tip>
        要将跟踪数据记录到特定项目，可以在 `OTEL_EXPORTER_OTLP_HEADERS`
        环境变量中添加 `projectName` 参数：

        ```bash
        export OTEL_EXPORTER_OTLP_HEADERS='projectName=<your-project-name>'
        ```

    </Tip>
    </Tab>

</Tabs>

## 在 Smolagents 中使用 Opik

要跟踪您的 Smolagents 应用程序，需要配置 OpenTelemetry 来检测框架：

```python
# Import telemetry components
from opentelemetry.sdk.trace import TracerProvider
from opentelemetry.sdk.trace.export import SimpleSpanProcessor

# Import SmolAgents instrumentation
from openinference.instrumentation.smolagents import SmolagentsInstrumentor

# Import OTLP exporter
from opentelemetry.exporter.otlp.proto.http.trace_exporter import OTLPSpanExporter

# Set up the tracer provider with span processor
trace_provider = TracerProvider()
trace_provider.add_span_processor(
    SimpleSpanProcessor(OTLPSpanExporter())
)

# Instrument SmolAgents
SmolagentsInstrumentor().instrument(tracer_provider=trace_provider)

# Now use SmolAgents as usual
from smolagents import CodeAgent, WebSearchTool, OpenAIServerModel

model = OpenAIServerModel(model_id="gpt-4o")
agent = CodeAgent(
    tools=[WebSearchTool()],
    model=model,
    stream_outputs=True
)

agent.run("How many seconds would it take for a leopard at full speed to run through Pont des Arts?")
```

<Frame>
  <img src="/img/cookbook/smolagents_trace_cookbook.png" alt="Smolagents 跟踪" />
</Frame>

## 进一步改进

如果您希望我们改进此集成，请在 [Github](https://github.com/comet-ml/opik/issues) 上提交新的功能请求。
